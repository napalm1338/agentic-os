# Jarvis

Voice mode for [[agentic-os]]. Push-to-talk (then a wake word), an
intent router (brief / build / memory / system / chat), and spoken
replies in a calm British voice. The visual is an arc-reactor core
that lights up when listening and pulses to its words when speaking.

"Jarvis, remember…" writes straight into the [[memory-galaxy]].
