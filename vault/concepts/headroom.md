# Headroom

The token monitor for [[agentic-os]]: a 5-hour rolling window of
usage per agent and model, a marquee bar with a projection of where
the window will land, and a Token Saver policy that keeps briefs
lean without cutting validation or safety.
